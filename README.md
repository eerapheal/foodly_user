# foodly_user
# reselling is completely forbidden 
# if you resell the code you would be fined 10,000 usd 

If you compile for web you need enable the comment out part in main.dart and order_contronller.dart
Or you can run the script first disable_code.py
This is disable the web code for mobile build
run 'python disable_code.py'
Before you run the above code make sure you set the flag to true
disable_code_flag = True 
